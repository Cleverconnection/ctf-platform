name: Object Storage Exposto
category: Web
value: 20
description: |-
  <p><strong>Cenário:</strong></p>
  <p>
    O serviço de distribuição de arquivos concatena o caminho solicitado diretamente com o diretório raiz. Com isso,
    objetos privados ficam acessíveis via path traversal.
  </p>
  <p>
    <code>Recurso / Acesso:</code>
    <strong>Gateway de objetos (container individual)</strong>
  </p>
  <p><strong>Objetivo:</strong><br>Baixar <code>private/flag.txt</code> manipulando o parâmetro <code>path</code>.</p>
  <hr>
  <p>Normalize caminhos antes de servir arquivos estáticos.</p>
attribution: ""
type: container
extra:
  initial: 20
  decay: 0
  minimum: 20
connection_info: Container
flags:
  - ITAU2025{object_storage_flag}
hints:
  - content: >-
      Requisite <code>?path=../private/flag.txt</code> para escapar da pasta pública.
    cost: 5
